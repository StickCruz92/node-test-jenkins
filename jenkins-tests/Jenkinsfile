pipeline {
  agent any
  tools {
    nodejs '12.10.0'
  }

  options {
    timeout(time: 2, unit: 'MINUTES')
  }

  stages {
    stage('Install dependencies') {
      steps {
        echo "Hello World!"
      }
    }
    stage('Run tests') {
      steps {
        echo "Hello World! 2"
      }
    }
  }
}
